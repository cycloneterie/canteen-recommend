# 四食堂菜品推荐系统 —— 项目研究结题报告

---

## 一、课题研究目的

本项目旨在解决高校学生在食堂用餐时面临的"选择困难症"——面对食堂几十个窗口、上百道菜品，学生往往不知道该吃什么。通过构建一个智能化的菜品推荐系统，帮助用户根据个人口味偏好、预算、健康需求等多维度条件，快速筛选并推荐最适合的当日菜品，提升用餐决策效率与满意度。

具体目标包括：
1. 建立涵盖四食堂全部窗口的菜品数据库（共 77 道菜）
2. 实现多维度的灵活筛选功能（品类、烹饪方式、辣度、预算、饱腹感、健康偏好等）
3. 设计场景化推荐模式（快速简餐、满足一餐、轻食主义、犒劳自己等）
4. 提供智能匹配度评分算法，确保推荐结果可信可用
5. 实现搜索、排序、随机推荐等辅助功能

---

## 二、课题背景

高校食堂是学生日常用餐的主要场所，但随着食堂规模的扩大和菜品种类的丰富，学生在选择时往往面临信息过载的问题。以本课题涉及的四食堂为例，共有 20 余个窗口、77 道以上菜品，分布在三层楼中。传统做法是学生逐个窗口浏览，这既低效又容易错过心仪菜品。

与此同时，AI 辅助编程工具的快速发展为个人开发者提供了前所未有的生产力提升。腾讯云推出的 **CodeBuddy** AI 编程助手，基于混元大模型与 DeepSeek 双核驱动，支持自然语言对话式编程、代码生成、智能补全等功能，能够显著降低开发门槛、提升开发效率。本课题正是利用 CodeBuddy 作为核心开发工具，从零到一快速构建了这一完整可用的 Web 应用。

---

## 三、课题研究主要内容

### 3.1 系统架构

本系统采用**纯前端单页应用（SPA）**架构，仅由一个 `index.html` 文件组成，内嵌 CSS 样式与 JavaScript 逻辑，无需任何后端服务器、数据库或构建工具。部署方式为 GitHub Pages 静态托管，实现零成本上线。

**技术栈：**
- HTML5：页面结构
- CSS3：响应式布局、渐变配色、动画效果
- Vanilla JavaScript（原生JS）：所有业务逻辑，包括筛选、匹配评分、搜索、排序

**系统功能模块：**
- **菜品数据库**：77 道菜的 JSON 数组，包含名称、窗口、类别、烹饪方式、辣度、价格、饱腹感、健康标签、评分、热度、是否新品等字段
- **快捷场景选择**：6 个预置场景按钮，一键应用筛选组合
- **多维筛选器**：9 类筛选条件（品类、烹饪方式、温度、辣度滑块、预算、饱腹感、健康偏好、推荐维度、搜索框）
- **智能匹配评分算法**：对各筛选条件加权计分，输出 0-100 的匹配度
- **排序与展示**：支持按匹配度、热度、评分、价格排序，卡片式结果展示

### 3.2 使用的 AI 工具：CodeBuddy

本课题全程使用 **腾讯云 CodeBuddy IDE** 进行开发。CodeBuddy 提供了智能对话、代码生成、上下文感知等能力，是本项目得以高效完成的关键工具。

**主要使用的 CodeBuddy 功能：**
- **对话式编程（Agent 模式）**：通过自然语言描述需求，CodeBuddy 自动生成代码并迭代优化
- **智能补全**：在编写 HTML/CSS/JS 时提供毫秒级代码预测
- **上下文理解**：CodeBuddy 能够理解整个项目文件的上下文，在多处修改时保持一致性
- **批量操作**：一次性修改多处数据（如同时修正 5 道菜的 `isHot` 字段）

### 3.3 关键 AI 提示词示例

以下是开发过程中使用的一些关键提示词（Prompt）：

**提示词 1：初始项目搭建**
> "帮我做一个四食堂菜品推荐系统，包含77道菜的数据，支持按品类、烹饪方式、辣度、预算、饱腹感、健康偏好筛选，有快捷场景选择和智能推荐功能。"

**提示词 2：搜索功能添加**
> "上面五个意面和烤肉拌饭是比较火的，其余的可以当做不火处理。然后可以加一个搜索框来实现你说的查找具体菜品改进"

**提示词 3：数据修正与优化**
> "请根据常识更新全部 77 道菜的 isHot、health、rating 等字段数据，确保符合实际食堂情况。"

**提示词 4：本报告生成**
> "帮忙完成一下项目研究结题报告，大约1000字。参考资料可以找一点例如codebuddy使用文档，龙虾部署文档等等。"

这些提示词展示了 CodeBuddy 在实际开发中从项目搭建、功能迭代到文档撰写全流程的辅助能力。

---

## 四、问题与建议、体会与收获

### 4.1 开发过程中遇到的问题及解决方案

**问题 1：菜品数据量大，手动录入效率低**
77 道菜的数据包含多个字段，手工编写 JSON 数据极易出错且耗时。**解决方案：**利用 CodeBuddy 的批量生成能力，通过自然语言描述菜品信息，让 AI 批量生成结构化的 JSON 数据，大幅提升了数据录入效率。

**问题 2：`isHot` 标记不准确**
初始数据中部分菜品的热门标记与实际不符（如番茄肉酱意面、韩式烤肉拌饭等确实热门但未标记）。**解决方案：**通过对话方式让 CodeBuddy 逐项检查并修正数据，同时利用 `replace_in_file` 工具精确定位修改。

**问题 3：搜索功能与现有筛选逻辑的融合**
新增搜索框后需要与已有的多维筛选器协同工作，确保两者过滤条件同时生效。**解决方案：**在筛选状态对象中新增 `searchText` 字段，在 `getRecommendations` 函数中添加独立的搜索过滤逻辑层，搜索匹配菜品名称、窗口、标签、描述四个维度。

**问题 4：单文件代码过长，维护困难**
随着功能迭代，`index.html` 文件已超过 2700 行。**解决方案：**保持代码块注释清晰、函数职责单一。虽然当前项目规模尚可接受，但建议后续可拆分为多个 JS/CSS 文件以提升可维护性。

### 4.2 体会与收获

1. **AI 辅助开发极大提升了效率**：从零到一构建一个包含 77 道菜数据、多种筛选逻辑的完整 Web 应用，在 CodeBuddy 的协助下仅需数小时即可完成，传统方式可能需要数天。

2. **自然语言编程降低了技术门槛**：即使不熟悉某些 CSS 动画或 JS 算法细节，通过自然语言描述需求，AI 也能生成高质量代码，这让非专业开发者也能完成复杂项目。

3. **数据质量是推荐系统的核心**：匹配算法的优劣决定了用户体验。本项目通过多维度评分机制和合理的权重分配，确保了推荐结果的可信度。

4. **迭代式开发是最佳实践**：本项目经历了"基础搭建 → 功能完善 → 数据修正 → 用户体验优化"四个迭代周期，每次迭代都有明确的改进目标。

---

## 五、参考资料

| 序号 | 资料名称 | 链接/来源 |
|------|----------|-----------|
| 1 | CodeBuddy 官方文档 - 概述与使用指南 | https://www.codebuddy.ai/docs/zh/ide/User-guide/Overview |
| 2 | 腾讯云 CodeBuddy 产品概述 | https://cloud.tencent.com/document/product/269/104236 |
| 3 | CodeBuddy AI IDE 使用入门指南（腾讯云开发者社区） | https://cloud.tencent.com/developer/article/2550112 |
| 4 | 国产 AI 编程神器 CodeBuddy 完整攻略 | https://developer.cloud.tencent.com/article/2563608 |
| 5 | 腾讯云轻量应用服务器 Lighthouse 产品文档 | https://cloud.tencent.com/document/product/1207/44361 |
| 6 | GitHub Pages 静态网站部署教程 | https://docs.github.com/zh/pages |
| 7 | MDN Web 文档 - HTML/CSS/JavaScript 参考 | https://developer.mozilla.org/zh-CN/ |
| 8 | 四食堂菜品调研数据（自行采集） | 校园实地调研 |
